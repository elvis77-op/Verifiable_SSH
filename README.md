## steps to deploy verifiable_ssh:
    1. define the address and port of TDVM in main.sh
    2. run "./build_vssh.sh"
    3. run "./launch_vssh.sh"

## to remove the keys of veriafiable_ssh, run "./clean_vssh.sh"

## trubleshooting

### installation process in docker time out
#### add proxy in Dockerfile
